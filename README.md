# Portfolio
A web app built in React to showcase my projects.

## Run app locally
- Make sure NPM is installed
- In the terminal, navigate to the project directory and type `npm run dev`

## Build app for production
- In the terminal, navigate to the project directory and type `npm run build`
